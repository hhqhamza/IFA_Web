import React, {Fragment} from "react";
import Playerr from "../components/Player";

const chatroom = () => {
    return (
        <Fragment>
        <h1 className="app" >Chatroom page </h1>
        <Playerr/>
        </Fragment>
    );
};

export default chatroom;